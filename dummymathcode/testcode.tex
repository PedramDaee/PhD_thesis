Sample source code listing to test monospace font.

\begin{lstlisting}[basicstyle=\ttfamily,language=Pascal,frame=single]  % Start your code-block
for i:=maxint to 0 do
begin
{ do nothing }
end;
Write('Case insensitive ');
Write('Pascal keywords.');
\end{lstlisting}